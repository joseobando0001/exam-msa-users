spring:
  r2dbc:
   url: r2dbc:h2:mem:///${DB_NAME}
   username: ${DB_USER}
   password:
     driver: h2
  h2:
    console:
      enabled: true
  web-flux:
    base-path: /api/v1/
management:
  endpoints:
    web:
      base-path: actuator